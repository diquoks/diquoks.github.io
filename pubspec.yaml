name: diquoks_web
description: "Скоро превратится в сайт-визитку, но до этого ещё далеко..."
homepage: "https://diquoks.github.io"
repository: "https://github.com/diquoks/diquoks.github.io"
publish_to: none
version: 1.0.7+1

environment:
  sdk: ^3.13.0

dependencies:
  jaspr: ^0.23.4
  jaspr_router: ^0.9.0
  pub_semver: ^2.2.1
  pubspec_parse: ^1.6.0

dev_dependencies:
  build_runner: ^2.15.1
  build_web_compilers: ^4.8.5
  jaspr_builder: ^0.23.4
  jaspr_lints: ^0.7.2
  lints: ^6.1.0

jaspr:
  mode: static
  styles: standalone
